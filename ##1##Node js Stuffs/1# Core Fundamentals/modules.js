// impoting module using require method
// use const for importing module
const importedModuleObject = require("./lib");

// using object keys from imported module
console.log(importedModuleObject.name, importedModuleObject.sum(10, 20));
// //used to export any var
// module.exports.title = "Hello!";

//will console all the keys that contains mudule
// console.log(module);
